# Frontend

Simple React + Tailwind frontend for the SRM assignment.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

## Environment

```env
VITE_API_BASE_URL=http://localhost:3000
```

Set `VITE_API_BASE_URL` to your deployed backend URL when hosting.

